https://registry.terraform.io/providers/hashicorp/azurerm/latest/docs/resources/storage_data_lake_gen2_filesystem


terraform validate                          #validate hcl

terraform apply                             #apply changes being prompted to enter "yes"
terraform apply --auto-approve              #apply changes without being prompted to enter "yes"

terraform destroy                           #destroy/cleanup deployment changes being prompted to enter "yes"
terraform destroy --auto-approve            #destroy/cleanup deployment changes without being prompted to enter "yes"